import { DeleteFromFavoriteDirective } from './delete-from-favorite.directive';

describe('DeleteFromFavoriteDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteFromFavoriteDirective();
    expect(directive).toBeTruthy();
  });
});
