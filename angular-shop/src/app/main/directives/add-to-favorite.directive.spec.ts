import { AddToFavoriteDirective } from './add-to-favorite.directive';

describe('AddToFavoriteDirective', () => {
  it('should create an instance', () => {
    const directive = new AddToFavoriteDirective();
    expect(directive).toBeTruthy();
  });
});
